[phases.install]
cmds = [
  "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt && python -c \"open('/opt/venv/lib/python3.12/site-packages/PytorchWildlife/models/detection/__init__.py', 'w').write('from .ultralytics_based import *\\n')\" && pip uninstall -y opencv-python opencv-python-headless && pip install --force-reinstall --no-cache-dir opencv-python-headless"
]
